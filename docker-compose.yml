version: "3.9"
services:
  go:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    tty: true
    command: /bin/sh -c "air -c .air.toml"
